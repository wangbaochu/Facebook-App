======================================================================================
Notice:
In order to build a real work APK, you should replace your Android debug.keystore with this debug.keystore. Copy it to replace the one on /Users/username/.android/debug.keystore. 

Because this app has been registered on Facebook Dashboard and Google API Console both with my this debug.keystore. 